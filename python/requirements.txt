Pillow
numpy
spidev
# GPIO: use system packages (avoids lgpio build). Run before venv:
#   sudo apt install python3-rpi-lgpio python3-libgpiod
# Then: python3 -m venv --system-site-packages .venv
# For gpiod button fallback when edge detection fails
gpiod
